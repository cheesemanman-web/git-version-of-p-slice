name: P-Slice iOS IPA (FINAL FIX)

on:
  workflow_dispatch:

jobs:
  ios:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Haxe
        run: |
          brew update
          brew install haxe
          haxe -version

      - name: Setup haxelib (SAFE PATH)
        run: |
          export HAXELIB_ROOT=$GITHUB_WORKSPACE/.haxelib
          mkdir -p $HAXELIB_ROOT
          haxelib setup $HAXELIB_ROOT

          haxelib install lime
          haxelib install openfl
          haxelib install flixel
          haxelib install flixel-addons
          haxelib install hxcpp
          haxelib install hscript
          haxelib install hscript-iris
          haxelib install tjson

          haxelib run lime setup

      - name: Fix Lime crash (CRITICAL)
        run: |
          export LIME_CONFIG_DIR=$GITHUB_WORKSPACE/.lime
          mkdir -p $LIME_CONFIG_DIR
          echo "<config />" > $LIME_CONFIG_DIR/config.xml

      - name: Debug project
        run: ls -R

      - name: Build iOS (NO SIGNING, SAFE MODE)
        run: |
          export HAXELIB_ROOT=$GITHUB_WORKSPACE/.haxelib
          export LIME_CONFIG_DIR=$GITHUB_WORKSPACE/.lime

          haxelib run lime build ios -verbose \
            -D CODE_SIGNING_REQUIRED=NO \
            -D CODE_SIGNING_ALLOWED=NO \
            -D AD_HOC_CODE_SIGNING_ALLOWED=NO

      - name: Package IPA
        run: |
          cd bin/ios/Release-iphoneos || exit 0
          mkdir -p Payload
          cp -r *.app Payload/
          zip -r P-Slice.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: P-Slice-iOS-IPA
          path: bin/ios/Release-iphoneos/P-Slice.ipa
