name: Build P-Slice iOS IPA (FIXED)

on:
  workflow_dispatch:

jobs:
  ios:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Haxe (stable fix)
        run: |
          brew update
          brew install haxe
          haxe -version

      - name: Setup Haxelib
        run: |
          haxelib setup ~/haxelib
          haxelib install lime
          haxelib install openfl
          haxelib install flixel
          haxelib install flixel-addons
          haxelib install hxcpp
          haxelib install hscript
          haxelib install hscript-iris
          haxelib install tjson
          haxelib run lime setup

      - name: Fix Lime crash (put_env fix)
        run: |
          export HOME=$RUNNER_TEMP
          mkdir -p $HOME/.lime
          echo '<config />' > $HOME/.lime/config.xml

      - name: Debug project files
        run: |
          ls -R

      - name: Build iOS (NO SIGNING)
        run: |
          export HOME=$RUNNER_TEMP
          haxelib run lime build ios -verbose \
            -D CODE_SIGNING_REQUIRED=NO \
            -D CODE_SIGNING_ALLOWED=NO

      - name: Package IPA
        run: |
          cd bin/ios/Release-iphoneos || exit 0
          mkdir -p Payload
          cp -r *.app Payload/
          zip -r PSlice.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: PSlice-IPA
          path: bin/ios/Release-iphoneos/PSlice.ipa
